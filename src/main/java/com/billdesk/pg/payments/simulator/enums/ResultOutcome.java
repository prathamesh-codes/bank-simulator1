package com.billdesk.pg.payments.simulator.enums;

/** The tester's chosen underlying result, independent of delivery mechanics (see {@link DeliveryMode}). */
public enum ResultOutcome {
  SUCCESS,
  FAILURE
}
